# 🧠 Habit Tracker API

Backend-часть SPA-приложения для трекинга полезных привычек, вдохновлённого книгой  
**«Атомные привычки» — Джеймс Клир** ✨

Проект реализован на **Django + Django REST Framework** и предназначен для работы с фронтендом и Telegram-ботом.

---

## 🚀 Возможности проекта

- 👤 Регистрация и авторизация пользователей
- ✅ CRUD-операции для привычек
- 🌍 Публичные и приватные привычки
- ⏰ Напоминания о привычках через Telegram
- 🔁 Гибкая периодичность выполнения
- 📄 Автоматическая документация API
- 🧪 Покрытие тестами ≥ 80%

---

## ✅ Валидаторы

- Нельзя одновременно указывать `reward` и `related_habit`
- В связанные привычки можно выбирать **только приятные**
- У приятной привычки **не может быть** награды или связанной привычки
- Время выполнения ≤ **120 секунд**
- Привычку нельзя выполнять реже, чем **1 раз в 7 дней**

---

## 🔐 Права доступа

- Пользователь имеет доступ **только к своим привычкам**
- Публичные привычки доступны всем **только для просмотра**
- Редактирование и удаление — только владельцем

---

## 📡 Эндпоинты

### Аутентификация
- `POST /users/register/` — регистрация
- `POST /users/login/` — авторизация

### Привычки
- `GET /habits/` — список привычек пользователя (с пагинацией)
- `GET /habits/public/` — список публичных привычек
- `POST /habits/` — создание привычки
- `PUT /habits/{id}/` — редактирование
- `DELETE /habits/{id}/` — удаление

---

## 📄 Пагинация

- Пагинация включена
- **5 привычек на страницу**

---

## 🤖 Интеграция с Telegram

Для отправки напоминаний используется Telegram-бот + Celery.


## ⏳ Отложенные задачи

- Используется **Celery**
- Напоминания отправляются согласно времени и периодичности привычки
- Брокер сообщений — Redis

---

## 🌐 CORS

- CORS настроен для подключения фронтенда
- Используется `django-cors-headers`

---

## 📚 Документация API

- Автогенерация документации (Swagger / Redoc)
- Документация доступна после запуска сервера

---

## ⚙️ Установка и запуск

```bash
git clone https://github.com/your-repo/habit-tracker.git
cd habit-tracker
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python manage.py runserver
celery -A config beat -l info  
celery -A config worker -l info -P eventlet  
